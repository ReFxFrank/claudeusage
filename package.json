{
  "name": "pulse-usage-dashboard",
  "version": "1.5.2",
  "description": "A local Claude Code usage dashboard. The server reads ~/.claude read-only and has zero runtime dependencies; the React frontend (web/) is built to static files it serves. Runs on http://localhost:4747.",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "pulse": "server.js"
  },
  "scripts": {
    "start": "node server.js",
    "build": "npm --prefix web install && npm --prefix web run build",
    "dev": "npm --prefix web run dev",
    "make-exe": "node build/make-exe.mjs",
    "inspect-schema": "node server.js --inspect-schema"
  },
  "keywords": [
    "claude",
    "claude-code",
    "usage",
    "dashboard",
    "monitor",
    "local",
    "zero-dependency"
  ]
}
